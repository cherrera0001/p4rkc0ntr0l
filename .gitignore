# dependencias
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/versions

# testing
/coverage

# Next.js
/.next/
/out/
next-env.d.ts

# producción
/build
/dist

# variables de entorno — NUNCA versionar secretos (spec.md §7)
.env
.env.*
!.env.example

# Vercel
.vercel

# Drizzle: las migraciones y sus snapshots SÍ se versionan — drizzle-kit los
# necesita para calcular el diff del próximo cambio de esquema.

# TypeScript
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editores / SO
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

.vercel
.env*
# `vercel link` agregó `.env*` al final, lo que anulaba la excepción de arriba y
# dejaba la plantilla fuera del repo. Se reinstaura: la plantilla SÍ se versiona,
# los valores nunca.
!.env.example

# Estado local de los verificadores (linea base de artefacto/version de INT-12).
# Es especifico de la maquina y del origen medido: una linea base commiteada
# produce FAIL confusos en un clon nuevo.
scripts/estado/

# Sondas temporales de los agentes de auditoria del concilio (no son del producto)
_audit_*.mjs
# Sondas de ruta que dejan los agentes de auditoria (no son del producto)
src/app/api/_sonda*/
